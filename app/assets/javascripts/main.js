$(document).ready(function() {
  $("#graph-btn").on("click", graph_update);
  $("#links-btn").on("click", links_update);
});

function graph_update() {
  console.log( "UPDATE GRAPH");

  // get date, start and end time and
  // make ajax request for data

  // update graph with top 10 reps
  return false;
}

function links_update() {
  console.log( "UPDATE LINKS");

  // get date, start and end time and
  // make ajax request for data

  // update links table with top 25 links
  return false;
}